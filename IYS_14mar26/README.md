# Database snapshots (local only)

Large dumps in this folder are **gitignored** (`.sql`, `.dmp`, `.tar.gz`, `arangodbdump14mar26/`).

Copy them here manually on each machine. Use `my-skills-plus-develop/import_from_dump.py` for PostgreSQL-style dumps into SQLite when needed.
